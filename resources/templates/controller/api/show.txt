   /**
     * Show details about a @@modelName@@
     *
     * @Get("/{id}")
     *
     * @Versions({"v1"})
     *
     * @Response(200, body={
        "data": {}
        })
     */
    public function show(@@requestClass@@ $request, @@modelName@@ $@@modelNameParam@@)
    {
      return $this->response->item($@@modelNameParam@@, new UserTransformer());
    }