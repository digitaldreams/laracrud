    /**
     * Create a @@modelName@@
     *
     *
     * @Post("/store")
     *
     * @Versions({"v1"})
     *
     * @Request("{"key":"value"}", contentType="application/x-www-form-urlencoded")
     *
     * @Response(200, body={})
     */
    public function store(@@requestClass@@ $request)
    {
        $model=new @@modelName@@;
        $model->fill($request->all());

        if ($model->save()) {
            session()->flash('app_message', '@@modelName@@ saved successfully');
            return $this->response->item($model, new UserTransformer());
        } else {
              return $this->response->errorInternal('Error occured while saving @@modelName@@');
        }
    }