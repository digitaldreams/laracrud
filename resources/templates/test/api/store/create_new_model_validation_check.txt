  /**
     * @test
     *
     * @dataProvider createNew@@modelShortName@@ValidationProvider
     *
     * @param $fieldName
     * @param $fieldValue
     */
    public function create_new_@@modelShortName@@_validation_check($fieldName, $fieldValue): void
    {
        $user = $this->createUser();

        $response = $this->actingAs($user)->postJson(@@route@@, [
            $fieldName => $fieldValue,
        ]);
        $response->assertStatus(Response::HTTP_UNPROCESSABLE_ENTITY)->assertJsonValidationErrors($fieldName);
    }
