
    /**
     * @test
     *
     * @dataProvider updateExisting@@modelShortName@@ValidationProvider
     *
     * @param $fieldName
     * @param $fieldValue
     */
    public function update_existing_@@modelShortName@@_validation_check($fieldName, $fieldValue): void
    {
        $user = $this->createUser();

        @@modelVariable@@ = @@modelShortName@@::factory()->for($user)->create();

        $response = $this->actingAs($user)->patchJson(@@route@@, [
            $fieldName => $fieldValue,
        ]);
        $response->assertStatus(Response::HTTP_UNPROCESSABLE_ENTITY)->assertJsonValidationErrors($fieldName);
    }
